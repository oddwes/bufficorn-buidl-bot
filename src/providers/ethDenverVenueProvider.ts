import { Provider } from "@elizaos/core";

export const ethDenverVenueProvider: Provider = {
    get: async () => {
        return "Venue details: National Western Stock Show, 4655 Humboldt Street, Denver, CO 80216";
    },
};

